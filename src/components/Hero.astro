---
const {
  title,
  sub,
  eyebrow = 'Storewise',
} = Astro.props;
---
<section class="hero">
  <div class="heroInner">
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    {sub && <p class="heroSub">{sub}</p>}
  </div>
</section>

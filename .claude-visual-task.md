# Storewise 视觉增强任务（委派给 Claude Code）

## 项目背景
Storewise 是 /tmp/node-web 下的 Astro 静态站（App Store 付费决策双语站 souk.eu.org）。
样式集中在 src/style.css（125KB 单文件），页面模板在 src/pages/ 和 src/layouts/。

## 已确认的单调问题（必须解决，按优先级）
1. **sideIcon 只剩单字母**：侧边栏分类图标退化成了 R/S/K/G/!/C/T 单个字母，视觉枯燥。
   要求：把每个分类替换为**有语义的 emoji 或更具辨识度的图形符号**，并在 CSS 里给每个分类
   加独立的柔和底色（不能只用 #ff6b9b 一种粉色）。
2. **文章卡片同质化**：45 篇文章卡片全部同款样式，无视觉差异。
   要求：为每个分类定义 accent 颜色变量（如 .cat-Refunds{--cat:#ff6b9b}），卡片顶部加
   分类彩色小条或者分类徽章着色，让不同分类在列表页一眼可分。
3. **系列页缺进度感**：阅读系列（series/）7 个系列无进度提示。
   要求：在系列索引页给每篇文章加"第 N 篇 / 共 M 篇"标记（数据已有 series.items 和 position）。
4. **hero 区分度低**：首页 hero 与区块缺少层次。
   要求：给 hero 增加渐变边框或装饰性背景图案（保持现有暗色主题兼容），hero 与下方 section
   使用不同层次背景（现有 .hero 已有 radial-gradient，可增强但不改布局）。

## 硬性约束（违反即失败）
- **不要改 HTML 结构**（aside+main 布局、.shell/.articleLayout 网格）——这是用户拍板的布局。
- **不要动正文内容**（articles.js 数据不动）。
- **不要删除现有 CSS 规则**，只允许追加新规则或修改选中元素的外观属性。
- **保持暗色模式兼容**：所有新增颜色必须在 body.dark 下有对应值。
- **保留 max-width:none 的自适应布局**——不要重新引入 max-width 容器。
- 侧边栏宽度 286px 固定，不要改宽。
- 移动端 @media(max-width:760px) 规则不要破坏。

## 实施步骤
1. 先读 src/style.css 中 aside/sideIcon/sideGroup/card/articleCard/seriesIndexCard 相关规则。
2. 查看 src/data/related.js 的 ARTICLE_SERIES 和 src/data/articles.js 的分类列表，
   确认有哪些分类（Refunds/Security/Payments/Family/Subscriptions/Privacy/Support/Purchasing/Safety/Regions/Workflow/Account Safety/App Safety）。
3. 找到 sideIcon 在模板中的生成位置（sideGroup 链接循环），确认是否有 icon 字段可用；
   若无，用 CSS 基于分类名的 :before 内容替换（例如 .sideGroup a[href*="refund"] .sideIcon::before{content:"↩"}）。
4. 实现分类 accent 色变量 + 卡片分类着色。
5. 实现系列页进度标记。
6. 增强 hero 视觉层次（渐变边框或装饰）。
7. 运行 npm run build 验证构建通过。
8. 总结改动，不要提交 git（由调用方提交）。

## 验收标准
- npm run build 通过
- 改动只涉及 src/style.css（或 + 少量组件模板）
- 暗色模式可用
- 移动端不破坏